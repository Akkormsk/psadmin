# Бэклог: единый жизненный цикл тендера + локальный AI-агент

Дата: 2026-09-20, обновлено 2026-09-21. Собрано по итогам обсуждения: подготовительный
аудит проекта под внедрение локального AI-агента, разбор экономики каскада (реальные
модели/тарифы из `tenders/gateway_budget.py` и активной `CascadeConfigVersion`), и план
Кодекса по единой сущности тендера (см. `docs/tender_lifecycle_experiment.md` — этот
бэклог его расширяет и продолжает, не заменяет).

Файл теперь ведётся в рабочей ветке `tender-lifecycle` (worktree `psadmin2-lifecycle`),
не в `main` — вся дальнейшая работа по этому бэклогу коммитится сюда.

## Параллельный контекст

Локальная БД переведена на Postgres, чистая — миграции применены, среда рабочая
(`psadmin2-lifecycle`, свой venv, свой `.env`). Конфигурация активного каскада
(`CascadeConfigVersion`) на этой базе не перепроверялась — не полагаться на старое
чтение из `db.sqlite3` (шаги 1/2/4 — `gemini-3.1-flash-lite`, шаг 6 — `gpt-5.4-mini`)
без свежей проверки, когда дойдём до тюнинга каскада.

## Порядок работ

### 1. Канбан жизненного цикла тендера — реализовано и закоммичено
**Статус**: готово. Коммиты `8b869f8` (базовый канбан: `kanban()`,
`_found_tender_card`/`_estimate_card`, тумблер Канбан/Список, dispatch по
`?view=`), `39bc66c` (статус стадии крупнее справа + накапливаемые бейджи),
`5ea2ac7` (внесение итога торгов — своя секция на странице тендера, не на
карточке; нашли и исправили баг с вложенным `<form>`), `985ab2c` (фоновый
автоподтяг итога + канбан закреплён как дефолт для `tender_selection:list`,
9 старых тестов поправлены под новый дефолт). Проверено в браузере на
реальных данных и тестами (165/165 зелёных).

**Ещё не сделано из исходного объёма пункта 1** (не забыто, просто ещё в очереди):
- Плоский список (`?view=list`) всё ещё видит только `FoundTender` — не
  объединён с `TenderEstimate` в одну ленту по всем стадиям с быстрым
  фильтром. Канбан это уже умеет, список — ещё нет.
- Ручной дропдаун «интересно/не интересно» в плоском списке никуда не делся
  (список больше не дефолт, но сам не тронут). На канбане его и не было.
- Прогноз снижения %/источник как бейдж — ждёт пункта 2.

**Зависимости**: нет.

### 2. Чистка `ContractStat` / `price_stats_for` + прогноз в «Оценить»
**Зачем**: сейчас портит картину — в пул статистики попадают контракты с
несостоявшимся аукционом/единственным поставщиком (снижение 0-1% не потому что
рынок так решил, а потому что не было реальной конкуренции), и совпадение по
ОКПД2 слишком грубое. `price_stats_for` уже фильтрует `shared_purchase=False`,
но не фильтрует по числу участников — это и есть пробел.

**Объём**: фильтр `participants_count >= 2`; приоритет КТРУ над ОКПД2; поднять
вес текстового/ключевого совпадения. Вручную сверить 20-30 реальных сравнений
перед тем как полагаться на цифру. Затем — завести прогноз снижения как второй
результат кнопки «Оценить» (пункт 1), рядом с риск-анализом, одним действием.

**Зависимости**: нет (чистый код).

### 3. Единая сущность тендера (аддитивно, поэтапно)
Без изменений по существу — см. предыдущую версию бэклога. `Tender`,
`TenderDocumentImport`, `TenderItem` (происхождение каждого факта), `TenderRiskAssessment`,
`TenderCalculation` (снимок позиций на момент расчёта), `TenderOutcome` (участие,
итог торгов — даёт дом `ContractStat.is_ours`). Вводить рядом со старыми таблицами,
не одним коммитом. Первая фаза (`Tender`+`TenderDocumentImport`) может идти
параллельно с пунктом 1 — канбан потом просто переключит источник данных.

### 4. Тюнинг каскада — шаг 6
**Статус**: черновая правка (убрано поле причины `"w"` из промпта шага 6, 80
тестов зелёные) сделана в этом же обсуждении, **но на другой ветке до
переключения на `tender-lifecycle`** — не перенесена сюда, нужно решить,
переносить ли и когда. Шаг 5 не трогаем, пока не решим иначе отдельно.

### 5. Разморозка маршрута производства через базу знаний
Без изменений — `_retrieve_lessons` + `calculator.services.calculate_sheet_estimate`
как первая попытка без AI, модель только для новых случаев, подтверждённое —
обратно в `Lesson`.

### 6. Калькулятор как сервис-функция
Без изменений — вынести резолвинг строк из `calculator/views.py:44-59` в сервис.

### 7. Вердикт-механика (ROI + риски → релевантность тендера)
Без изменений по сути — но теперь явно строится поверх бейджей пункта 1 (не
отдельная новая визуализация, а агрегация того, что уже показано по карточке).
Зависит от пункта 2 (иначе считает по мусорным данным).

### 8. Автоматическое получение результата торгов + приоритет своей истории
**Статус**: половина сделана раньше срока (записана вместе с пунктом 1, так
получилось по ходу разговора про статусы/бейджи) — `TenderEstimate` получил
поля итога (`actual_price`, `actual_reduction_percent`, `outcome_checked_at`,
`outcome_source`), ручная кнопка «Забрать итог автоматически»/«Выиграли»/
«Проиграли» на странице тендера, и фоновый автоподтяг тем же демоном, что
собирает свежие тендеры. `ContractStat.is_ours` наконец получает первые
записи. Не полноценная модель `TenderOutcome` из пункта 3, а минимальный
срез тех же данных на существующей модели — сознательно, чтобы не тащить
весь entity-merge ради одной функции.

**Ещё не сделано**: приоритет своей истории внутри `price_stats_for` — прогноз
пока продолжает считаться только по общему рыночному пулу `ContractStat`,
даже если своя история уже накопилась. Переключить на «сначала своя история,
рынок — только как запасной вариант» имеет смысл после пункта 2 (там всё
равно трогаем `price_stats_for`).

### 9. Telegram как канал
Без изменений — последний пункт, тонкая обвязка поверх ядра из пунктов 5-6.

## Что не двигаем без отдельного разговора
- Шаг 5 каскада.
- Полное слияние `FoundTender`/`TenderEstimate` в одном коммите — только поэтапно.
- Локальный семантический поиск по каталогу (`var/semantic_index/*`,
  ветка `local/semantic-search`).
- Пункт 8, приоритет своей истории в `price_stats_for` — запись итога уже
  сделана, но переключать сам прогноз на свою историю раньше пункта 2 не
  будем, чтобы не сбивать последовательность.
